
! Trunk configuration on SW2 (multilayer switch)
enable
configure terminal

! Trunk toward SW1
interface GigabitEthernet1/0/1
 description Trunk to SW1 (Gi0/1)
 switchport trunk encapsulation dot1q
 switchport mode trunk
 switchport trunk allowed vlan 10,20,30
 no shutdown
exit

! If other trunk links exist add them similarly

end
write memory
